const Navbar = () => {
  return (
    <div className="flex justify-between items-start mb-6 sm:mb-8 md:mb-12">
      <span className="inline-block bg-black text-white text-xs font-semibold px-3 sm:px-4 py-1.5 sm:py-2 rounded-full">
        TARGET AUDIENCE
      </span>
      <span className="text-gray-900 text-xs font-semibold tracking-wider">
        ↘ DIGITAL BANKING PLATFORM
      </span>
    </div>
  );
};

export default Navbar;
